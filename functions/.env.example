# Mailtrap SMTP Configuration
# Get these credentials from your Mailtrap account at https://mailtrap.io

# SMTP Server Settings
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525

# Mailtrap Authentication
# Find these in: Mailtrap > Your Inbox > SMTP Settings > Integrations > Nodemailer
SMTP_USER=your_mailtrap_username_here
SMTP_PASS=your_mailtrap_password_here

# Email Configuration
# The email address that should receive admin notifications
ADMIN_EMAIL=admin@acbf.org.za

# The sender information for outgoing emails
FROM_EMAIL=noreply@acbf.org.za
FROM_NAME=ACBF

# Organization Details
ORGANIZATION_NAME=African Community Business Federation

# ============================================================
# SETUP INSTRUCTIONS
# ============================================================
#
# 1. Copy this file to .env in the same directory:
#    cp .env.example .env
#
# 2. Log in to your Mailtrap account at https://mailtrap.io
#
# 3. Navigate to: Your Inbox > SMTP Settings
#
# 4. Under "Integrations", select "Nodemailer"
#
# 5. Copy the credentials:
#    - Username (SMTP_USER)
#    - Password (SMTP_PASS)
#
# 6. Replace the placeholder values in your .env file with your actual credentials
#
# 7. Update ADMIN_EMAIL with the email address that should receive notifications
#
# 8. Update FROM_EMAIL and FROM_NAME if desired
#
# 9. NEVER commit the .env file to version control (it's in .gitignore)
#
# ============================================================
# PRODUCTION NOTES
# ============================================================
#
# For production use:
# - Replace Mailtrap with a production email service (SendGrid, Mailgun, AWS SES)
# - Update SMTP_HOST, SMTP_PORT, SMTP_USER, and SMTP_PASS accordingly
# - Ensure FROM_EMAIL uses a verified domain
# - The code structure remains the same, only credentials change
#
